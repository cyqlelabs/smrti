{
  "mcpServers": {
    "smrti": {
      "command": "smrti",
      "args": ["serve", "mcp"],
      "env": {
        "SMRTI_DB": "~/.smrti/memory.db",
        "SMRTI_PERSONALITY": "balanced",
        "SMRTI_AGENT_ID": "default"
      }
    }
  }
}
